# Gradual indexing rollout — 2 pages/day

Tracks the noindex-removal schedule for the 16 Alexandria/Cairo geo pages + `/printing/brochures`.
See plan: `~/.claude/plans/glimmering-dancing-planet.md`. Each day: remove `robots="noindex, nofollow"`
from that day's page(s), add them to `PAGES` in `astro/src/lib/sitemap-data.ts` (and drop them from
`NOINDEX_PATHS` there), commit, push, deploy via `scripts/deploy-astro.sh`, verify with curl.

- [x] Day 1 — `/alexandria`, `/cairo` (hubs) — also added nav dropdown + footer links
- [x] Day 2 — `/alexandria/ac`, `/cairo/ac`
- [x] Days 3-8 — remaining 12 vertical geo pages (cameras, cash-machines, copiers, office-supplies, printing, office-setup × both cities) — owner requested indexing all at once instead of pacing 2/day, done 2026-08-09
- [x] Day 9 — `/printing/brochures` — turned out to be a full built-out page, not a soft-redirect (stale assumption from the original plan doc); indexed 2026-08-09

All pages in the original 17-page noindex list are now indexed. `NOINDEX_PATHS` is empty.
